<h1> Post Show Page </h2>


Title: <%= @post.title %>
Content: <%= @post.content %>


Categories: 
<% @post.categories.each do |c| %>
    <p><%= c.name %> </p>
<% end %></br>


Comments: 
(All comments associated with post.)

<% @post.comments.each do |comment| %>
    <p><%= comment.user.username %> says: <%= comment.content %> </p>
<% end %> </br></br>
<%# 

(A list of all the unique users who have commented on the post. A user's name should only 
show up once in this section, even if they've commented multiple times.)</br>

- ran .uniq before even iterating  %>
Users: 

<% @post.users.uniq.each do |user| %>
    <p><%= link_to user.username, user_path(user) %> </p>
<% end %></br></br>


<%# (A form to add a new comment) - we're creating id so it knows about th comment ?  %>

<%= form_for Comment.new do |f| %>
    <%= f.hidden_field :post_id, value: @post.id %>

    <%= f.label :content %>
    <%= f.text_field :content %>
    </br>
    </br>

<h4> Select A User: </h4>
  <%= f.collection_select :user_id, User.all, :id, :username, include_blank: true %>
</br>

<h4> Enter Username: </h4>

    <%= f.fields_for :user, User.new do |u| %>
    <%= u.label :username %>
    <%= u.text_field :username %>
    <% end %>
    <%= f.submit %>
<% end %>